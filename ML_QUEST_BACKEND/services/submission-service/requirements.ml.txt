# Python packages required by the ML Submission worker
# Install on the host or inside the Docker container running ml.worker.ts:
#
#   pip3 install -r requirements.ml.txt

numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
scipy>=1.11
